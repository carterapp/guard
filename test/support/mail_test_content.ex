defmodule Doorman.MailTestContent do

  def welcome_subject(locale, meta) do 
    "hello"
  end

  def welcome_html_body(locale, meta) do
    "hello"
  end
  def welcome_text_body(locale, meta) do
    "hello"
  end

end
